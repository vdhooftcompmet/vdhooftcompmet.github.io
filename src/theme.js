import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light"
  },
  typography: {
    // tweak global typography here
  }
});

export default theme;
